---

import AdminLayout from "../../layouts/AdminLayout.astro";
import "../../styles/admin.css";
---

<AdminLayout title="Admin - Sobre Mi">
  <div class="min-h-screen bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900">
    <!-- Header -->
    <header class="admin-navbar">
      <div class="max-w-7xl mx-auto px-4 py-4 flex justify-between items-center">
        <h1 class="section-title-admin">📝 Gestionar Sobre Mi</h1>
        <a
          href="/admin/dashboard"
          class="text-slate-400 hover:text-white transition-colors"
        >
          ← Volver
        </a>
      </div>
    </header>

    <main class="max-w-7xl mx-auto px-4 py-12">
      <div class="form-container-admin max-w-2xl">
        <h2 class="text-2xl font-bold text-slate-100 mb-8">✨ Información Personal</h2>
        <form id="aboutForm" class="space-y-6">
          <div class="form-group-admin">
            <label>Tu Nombre / Título *</label>
            <input
              type="text"
              id="title"
              class="admin-input"
              placeholder="Mi Nombre Profesional"
            />
          </div>

          <div class="form-group-admin">
            <label>Descripción / Bio *</label>
            <textarea
              id="description"
              class="admin-textarea"
              style="min-height: 200px;"
              placeholder="Cuéntame sobre ti, tu experiencia, tus logros y pasiones..."
            ></textarea>
          </div>

          <div class="form-group-admin">
            <label>URL de Imagen de Perfil</label>
            <input
              type="url"
              id="profileImage"
              class="admin-input"
              placeholder="https://..."
            />
          </div>

          <button
            type="submit"
            class="w-full btn-primary-admin"
          >
            💾 Guardar Cambios
          </button>
        </form>

        <div id="error" class="mt-6 alert-admin alert-error hidden"></div>
        <div id="success" class="mt-6 alert-admin alert-success hidden"></div>
      </div>
    </main>
  </div>

  <script>
    const token = localStorage.getItem("token");
    if (!token) {
      window.location.href = "/admin-login";
    }

    const form = document.getElementById("aboutForm") as HTMLFormElement;
    const errorDiv = document.getElementById("error") as HTMLDivElement;
    const successDiv = document.getElementById("success") as HTMLDivElement;

    async function loadAbout() {
      try {
        const response = await fetch("http://localhost:4000/api/about");
        const data = await response.json();

        if (data.title) {
          (document.getElementById("title") as HTMLInputElement).value = data.title;
          (document.getElementById("description") as HTMLTextAreaElement).value =
            data.description;
          if (data.profileImage) {
            (document.getElementById("profileImage") as HTMLInputElement).value =
              data.profileImage;
          }
        }
      } catch (error) {
        console.error("Error loading about:", error);
      }
    }

    form.addEventListener("submit", async (e) => {
      e.preventDefault();
      errorDiv.classList.add("hidden");
      successDiv.classList.add("hidden");

      const title = (document.getElementById("title") as HTMLInputElement).value;
      const description = (document.getElementById("description") as HTMLTextAreaElement).value;
      const profileImage = (document.getElementById("profileImage") as HTMLInputElement).value;

      if (!title || !description) {
        errorDiv.textContent = "Todos los campos son requeridos";
        errorDiv.classList.remove("hidden");
        return;
      }

      try {
        const response = await fetch("http://localhost:4000/api/about", {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            Authorization: `Bearer ${token}`,
          },
          body: JSON.stringify({ title, description, profileImage }),
        });

        const data = await response.json();

        if (!response.ok) {
          errorDiv.textContent = data.error || "Error al guardar";
          errorDiv.classList.remove("hidden");
          return;
        }

        successDiv.textContent = "¡Cambios guardados exitosamente!";
        successDiv.classList.remove("hidden");
      } catch (error) {
        errorDiv.textContent = "Error de conexión";
        errorDiv.classList.remove("hidden");
      }
    });

    loadAbout();
  </script>
</AdminLayout>
